[package]
name = "usb-reset"
version = "0.1.0"
license = "AGPL-3.0-or-later"
authors = ["Max Maisel <max.maisel@posteo.de>"]
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
nix = { version = ">=0.29", features=["ioctl"] }
udev = ">=0.9"
